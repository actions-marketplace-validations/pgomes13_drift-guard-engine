# GitHub Actions

The recommended GitHub Actions integration is the [API Drift Agent](/api-drift-agent) — a LangGraph-powered agentic workflow that detects breaking API changes and automatically notifies affected consumer repos.
